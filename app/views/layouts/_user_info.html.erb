<% if current_user.present? %>
  <div id="user_info">
    <%= content_tag(:b, current_user.show_name) %><br><br>
    <ul>
      <%= content_tag(:li, link_to("Изменить данные", edit_user_registration_path)) %>
      <%= content_tag(:li, link_to("Выйти", destroy_user_session_path, method: :delete)) %>
    </ul>
  </div>
<% end %>
